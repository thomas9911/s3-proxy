
A S3 wrapper around OpenDal. In a separate database the metadata is stored, for access keys and if a bucket/file is private or public.

It should implement the standard s3 api and the presigned post request flow.

For a similar project see: F:\Rust\nats-s3
